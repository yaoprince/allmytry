server:
  port: 8088
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydatabase?useSSL=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    database: 0   #默认数据库
    host: 127.0.0.1
    port: 6379
    password:
    timeout: 5000
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
#mybatis:
#  type-aliases-package: com.myxuexi.testredismysql.model
#  mapper-locations: classpath:mapper/*.xml
